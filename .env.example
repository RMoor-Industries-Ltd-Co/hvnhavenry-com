# ─── HVN Havenry Environment Variables ───────────────────────────────────────
# Copy this file to .env.local and fill in your actual values.
# NEVER commit .env.local — it is gitignored.

# Shopify Storefront API (optional — only needed if embedding cart/checkout)
# Get from: Shopify Admin → Settings → Apps → Develop apps → Storefront API
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN=hvnhavenry.myshopify.com
NEXT_PUBLIC_SHOPIFY_STOREFRONT_ACCESS_TOKEN=

# If using Shopify Buy Button (JS SDK) the token above is the one needed.
# Leave blank to use direct product page links instead (default mode).

# Vale, the public-facing concierge (src/lib/vale.ts, POST /api/vale).
# Public/unauthenticated by design -- safe because the client can only ever send a
# fixed promptKey + real productId, never free text. Optional: without this, Vale
# falls back to static on-brand replies instead of live generation.
ANTHROPIC_API_KEY=
VALE_MODEL=claude-sonnet-5

# Vale's interaction log + scheduled HVN<->AMG report cache (src/lib/db.ts).
# Without this, logging/reporting silently no-ops -- the public concierge still works.
DATABASE_URL=
POSTGRES_PASSWORD=

# Machine-to-machine auth for POST/GET /api/agent* (src/lib/agentAuth.ts) -- the shared
# secret Cappo (AMG) and ALLIE/ALLEN use to pull Vale's HVN<->AMG activity report or ask
# her a live question. Must match the AGENT_API_KEY/CAPPO/ALLIE side configures.
AGENT_API_KEY=
